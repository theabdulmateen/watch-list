// edit comment 
const editButtons = document.querySelectorAll(".edit-button");
const form = document.querySelector(".comments-content form");

for (let i = 0; i < editButtons.length; i++) {
    editButtons[i].addEventListener("click", () => {
    });
}